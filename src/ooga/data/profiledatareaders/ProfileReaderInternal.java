package ooga.data.profiledatareaders;

/**
 * @author caryshindell
 * This houses any non-external profile reader methods that are common across all file types
 * It is empty because currently there are no such methods. But we still should have an internal api for profile readers.
 */
public interface ProfileReaderInternal {
}
